import { Head, Main, NextScript } from "next/document";

const Document = () => {
  return (
    <html lang="ko">
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </html>
  );
};

export default Document;
